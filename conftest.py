"""Pytest configuration.

This file's mere presence at the project root puts the root on ``sys.path``
(pytest's rootdir conftest insertion), so tests under ``tests/`` can
``import mirror`` and the other top-level modules without an installed package.
"""
